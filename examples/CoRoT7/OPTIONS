# File containing parameters for DNest3
# Put comments at the top, or at the end of the line.
1	# Number of particles
5000	# new level interval
1	# save interval
5	# threadSteps - how many steps each thread should do independently before communication
1	# maximum number of levels
45	# Backtracking scale length (lambda in the paper)
70	# Strength of effect to force histogram to equal push. 0-10 is best. (beta in the paper)
1000	# Maximum number of saves (0 = infinite)
    # (optional) samples file
    # (optional) sample_info file
    # (optional) levels file
